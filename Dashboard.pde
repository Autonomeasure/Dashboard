import processing.net.Client;

Client client;

void setup() {
  size(1280, 720);
  background(0);
  surface.setResizable(true);
  //client = new Client(this, "localhost", 8000);
}

void draw() {
  
}
